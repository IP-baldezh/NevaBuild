import { AccountStub } from "@/components/account/AccountStub";

export default function OrganizerAccountPage() {
  return (
    <AccountStub
      title="Кабинет организатора"
      text="Кабинет организатора будет доступен позже."
    />
  );
}
