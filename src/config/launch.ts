/**
 * ВРЕМЕННЫЙ режим лендинга.
 * true  — показываем только страницу /about, всё остальное скрыто.
 * false — полный сайт (вернуть когда все страницы будут готовы).
 */
export const LANDING_MODE = true;
